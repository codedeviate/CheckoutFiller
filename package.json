{
  "name": "checkoutfiller",
  "version": "0.4.2",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Fill checkout test credentials from the right-click menu.",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "package": "npm run build && node package.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.0",
    "vitest": "^2.1.0"
  }
}
